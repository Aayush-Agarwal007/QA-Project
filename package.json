{
  "name": "taskflow-mini-tests",
  "version": "1.0.0",
  "description": "Automated API and UI test suite for TaskFlow Mini.",
  "scripts": {
    "test:api": "jest tests/api --runInBand",
    "test:ui": "playwright test tests/ui",
    "test:ui:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "@playwright/test": "^1.45.0"
  }
}
